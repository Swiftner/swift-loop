{
  "id": "concentric-squares",
  "name": "Concentric Squares",
  "description": "Nested square rings — Rows are the rings, Columns the points around each square's perimeter. The squircle map (cos/max(|cos|,|sin|)) traces a true square outline.",
  "tags": ["grid", "polar", "tiling"],
  "author": "@swiftner",
  "cols": 32,
  "rows": 5,
  "formulas": {
    "x": "x = cos(c * TAU / cols) / max(abs(cos(c * TAU / cols)), abs(sin(c * TAU / cols))) * (r + 1) * {x:26}",
    "y": "y = sin(c * TAU / cols) / max(abs(cos(c * TAU / cols)), abs(sin(c * TAU / cols))) * (r + 1) * {y:26}"
  }
}

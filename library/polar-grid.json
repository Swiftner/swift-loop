{
  "id": "polar-grid",
  "name": "Polar Grid",
  "description": "Concentric rings with evenly spaced spokes — `cols` spokes × `rows` rings.",
  "tags": ["polar", "radial", "grid"],
  "author": "@swiftner",
  "cols": 12, "rows": 5,
  "formulas": {
    "x": "x = cos(c * TAU / cols) * (r + 1) * {x:20}",
    "y": "y = sin(c * TAU / cols) * (r + 1) * {y:20}"
  }
}
